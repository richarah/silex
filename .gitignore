# Repo-wide ignores. Subdirectory rules live in core/.gitignore and
# sdk/.gitignore. Do NOT add an anchored /core rule here -- it would ignore
# the entire core/ tree.

.DS_Store
CLAUDE.md
*.swp
*.swo
*~
.vscode/
.idea/
.pytest_cache/

# Build outputs (each component also ignores its own)
build/
